#include <stdio.h>	
#include "bsp.h"	



int main(void){

    // Record MEMS MIC to buffer1

    // pre-processthe buffer1 data and store it in buffer2

    // pass the buffer2 data to the neural network and get the result in buffer3

    // post-process the buffer3 data and store it in buffer4

    // play the buffer4 data to the audio DAC and write it to the sd card
}